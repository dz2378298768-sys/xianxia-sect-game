import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xianxia.sectgame',
  appName: '修仙宗门管理',
  webDir: 'dist',
  backgroundColor: '#050811',
};

export default config;
